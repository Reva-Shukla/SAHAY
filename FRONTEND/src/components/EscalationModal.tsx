import React, { useState } from 'react';
import { AlertTriangle, X } from 'lucide-react';
import type { RiskLevel } from '../types';

interface Props { open: boolean; caseId: string; riskLevel: RiskLevel; onClose: () => void; onConfirm: (note: string) => void; }
export const EscalationModal: React.FC<Props> = ({ open, caseId, riskLevel, onClose, onConfirm }) => { const [note, setNote] = useState(''); if (!open) return null; return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/40 p-4"><div role="dialog" aria-modal="true" className="w-full max-w-md rounded-2xl bg-white p-5 shadow-2xl space-y-4"><div className="flex items-start justify-between"><div className="flex items-center gap-2"><AlertTriangle className="w-5 h-5 text-rose-600" /><div><h2 className="text-base font-bold text-slate-900">Escalate Case</h2><p className="text-xs text-slate-500">Case {caseId}</p></div></div><button type="button" title="Close" onClick={onClose} className="text-slate-500 hover:text-slate-800"><X className="w-4 h-4" /></button></div><div className="rounded-xl bg-rose-50 border border-rose-200 p-3 text-xs text-rose-900">Current risk level: <strong>{riskLevel}</strong>. Review the case and confirm the escalation note.</div><textarea value={note} onChange={(event) => setNote(event.target.value)} placeholder="Enter escalation note..." className="w-full min-h-24 rounded-xl border border-slate-200 p-3 text-xs focus:outline-none focus:border-indigo-500" /><p className="text-[11px] text-slate-500">Tele-MANAS support is available as a future connected pathway through the dashboard panel.</p><div className="flex justify-end gap-2"><button type="button" onClick={onClose} className="rounded-xl border border-slate-200 px-3 py-2 text-xs font-bold text-slate-700">Cancel</button><button type="button" disabled={!note.trim()} onClick={() => onConfirm(note.trim())} className="rounded-xl bg-rose-600 px-3 py-2 text-xs font-bold text-white disabled:opacity-50">Confirm escalation</button></div></div></div>; };
